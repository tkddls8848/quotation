# 공용 코어(quotation/)는 변환 규칙을 Rust 확장(quotation_rust)으로 부른다.
# 확장은 maturin 으로 만들어 설치한다 (README 의 개발 절차 참고).
#
#   maturin build --release -m rust/python/Cargo.toml --out target/wheels
#   pip install target/wheels/quotation_rust-*.whl

# 테스트에서 산출물을 열어 보는 데 쓴다. 변환 자체에는 쓰지 않는다.
openpyxl==3.1.5

# 테스트
pytest==9.0.2
