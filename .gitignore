.venv/
__pycache__/
*.pyc
.pytest_cache/
.acceptance-tmp/

# 빌드 산출물
build/
dist/

# 골든 .xlsx 변환 캐시 (tools/xls2xlsx.ps1 로 재생성 가능)
.cache/

# 변환 결과물
out/

# 실행 시 자동 생성되는 외부 템플릿 (원본은 견적서_template.xls,
# 배포본은 quotation/resources 에 있다)
/견적서_template.xlsx

# QuotationTool.spec 은 직접 작성한 빌드 정의이므로 추적한다.
# PyInstaller 가 자동 생성하는 spec 만 제외한다.
launcher.spec
