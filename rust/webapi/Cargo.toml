[package]
name = "quotation-webapi"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
quotation-core = { path = "../core" }
sha2 = "0.11.0"
zip = { version = "2.4.2", default-features = false, features = ["deflate"] }
