# 골든 대조에서 제외할 셀. 형식: SHEET!ADDR
#
# 원본 프로그램의 버그 또는 골든 파일의 수기 편집으로 판단되는 셀만 넣는다.
# 근거는 SPEC_CELLMAP.md §7 에 남길 것.

# X-ROIS: D20 하나만 글꼴이 돋움 9 이고 나머지 D열 전체는 Tahoma 9 다.
# 값은 '10GbE Optical Transceiver SFP+ SR' (33자). 최장 설명도 아니고
# (D45 는 65자인데 Tahoma), 한글도 없고, WrapText 도 동일하다.
# 이 셀만 골라낼 데이터상 근거가 없어 골든의 수기 편집으로 판단한다.
# FS5045 에는 같은 유형의 이상치가 없다.
SERVER 1!D20

# (이력) X-ROIS SERVER 1!E59:E65 를 '원인 불명 이상치'로 등록했다가 철회했다.
# H/W 구간에서 기준행이 N/C 이면 서브 수량을 '={수량}' 으로 쓴다는 규칙이
# 밝혀져 설명이 되었다.
