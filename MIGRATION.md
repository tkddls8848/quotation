# 전환 안내 — 기존 사용자용

2005년판 `견적서 자동화 프로그램` 에서 2026년판 `견적서 작성기` 로 넘어갈 때 알아야 할 것.

---

## 1. 무엇이 그대로인가

- **입력**: eConfig Export XML. 바꿀 것 없다.
- **출력 내용**: 시트 구성, 셀 위치, 값, 수식, 서식, 글꼴이 전부 동일하다.
  기존 견적서와 셀 단위로 대조해 차이가 없음을 확인했다.
- **화면 구성과 조작 순서**: XML 선택 → 옵션 → 변환.
- **오류 문구**: `CFData을 찾을수 없습니다.`, `할인율은 99보다 클수 없습니다.` 등 그대로다.

## 2. 무엇이 달라지는가

### 출력 확장자가 `.xlsx` 다
`.xls` → `.xlsx`. 내용은 같다. Excel 2007 이후 버전이면 그대로 열린다.
받는 쪽이 `.xls` 를 요구하면 Excel 에서 `다른 이름으로 저장 → Excel 97-2003 통합 문서` 를 쓰면 된다.

### 삼성 SDS B2B 양식이 없다
`optSDS` 모드는 제거되었다. IBM 양식만 생성한다.

### 유지정비료를 채우지 않는다
구버전은 XML 의 `MaintenanceUnitListPrice` 를 H열(제안가/유지정비료)에, 기간을 I열에
넣었다. 이제 넣지 않는다. H열은 표 구조(테두리·배경·병합)만 유지된 **빈 칸**으로 나오므로
필요하면 손으로 적으면 된다.

### 견적서 번호와 담당자는 템플릿에서 고친다
`견적서_template.xlsx` 가 EXE 옆에 자동으로 만들어진다. 이 화일을 Excel 로 열어 고친다.

| 고칠 것 | 위치 |
|---|---|
| 견적서 번호 `NO : Trialinfo-YY-` | TOTAL 시트 B2 셀 |
| `담당 : 시스템사업부 ○ ○ ○` | TOTAL 시트 상단 머리말 도형 |

견적서 번호는 **연도 두 자리만 자동 갱신**된다. 결과는 언제나 `NO : Trialinfo-26-`
형태이고 연도 뒤에는 아무것도 붙지 않는다.
담당자 이름은 도형 안에 있어 프로그램이 건드리지 않는다.

> 템플릿을 고친 뒤에는 저장만 하면 된다. 다음 변환부터 바로 반영된다.

### Excel 을 켜 둔 채 변환해도 된다
원본은 변환 전에 Excel 을 모두 종료해야 했다. 이제 그럴 필요가 없다.
Excel 을 쓰지 않고 화일을 직접 만들기 때문이다.

단, **변환하려는 견적서 화일 자체가 열려 있으면** 덮어쓸 수 없다.
이때는 그 화일만 닫으면 된다.

## 3. 설치와 제거

### 새 버전 설치
`QuotationTool.exe` 를 원하는 폴더에 두면 끝이다. 관리자 권한도, OCX 등록도 필요 없다.
Excel 이 없는 PC 에서도 동작한다.

### 구버전 제거
`제어판 → 프로그램 추가/제거` 에서 `견적서 자동화 프로그램` 을 제거한다.
새 버전과 함께 두어도 서로 간섭하지 않으므로, 한동안 병행 운영하다가 제거해도 된다.

### 설정 이관
구버전이 남긴 `견적서생성기.ini` 를 찾으면 최초 실행 시 자동으로 값을 옮긴다.
아래 위치를 확인한다.

```
%ProgramFiles%\Quotation\견적서생성기.ini
%ProgramFiles(x86)%\Quotation\견적서생성기.ini
%LOCALAPPDATA%\VirtualStore\Program Files\Quotation\견적서생성기.ini
```

> 세 번째 경로는 Windows 가 `Program Files` 쓰기를 막으면서 몰래 옮겨 둔 것이다.
> 구버전에서 설정이 저장되지 않는 것처럼 보였다면 이 때문이다.

새 설정은 `%LOCALAPPDATA%\QuotationTool\config.json` 에 저장된다.

## 4. 문제가 생기면

로그를 먼저 본다.

```
%LOCALAPPDATA%\QuotationTool\logs\quotation.log
```

일자별로 30일치가 남는다. 원본에는 로그가 없어 원인 파악이 불가능했다.

| 증상 | 확인할 것 |
|---|---|
| `화일을 저장하지 못했습니다` | 같은 이름의 견적서가 Excel 에 열려 있는지 |
| `CFXML을 찾을수 없습니다` | eConfig Export 로 받은 XML 이 맞는지 |
| `견적서 작성을 위한 Item을 찾을 수 없습니다` | XML 에 품목이 들어 있는지 |
| 금액이 다르다 | 로그의 `파싱 완료: 그룹 N, 라인 N` 과 XML 을 대조 |

## 5. 검증되지 않은 기능

**할인율**은 비교할 기존 견적서가 없어 동작만 정의한 상태다. 입력하면 `공급가` 행에
`총합계 x (1 - 할인율)` 수식을 넣는다. 입력하지 않으면 기존과 같이 공란이다.
실제로 쓰기 전에 기존 프로그램 결과와 한 번 대조하기를 권한다.

할인율을 비워 두면 유지정비료(제외 결정분)를 뺀 나머지는 기존 견적서와 완전히 같다.
